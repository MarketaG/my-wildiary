# My Wildiary

## Launch locally via Docker

1. run:

```bash
docker-compose up --build
```

2. Frontend: http://localhost:3000
3. Backend: http://localhost:4000

## Using MongoDB Atlas

1. Create a .env file with MONGODB_URI for Atlas
2. run:

```bash
docker-compose up --build
```
